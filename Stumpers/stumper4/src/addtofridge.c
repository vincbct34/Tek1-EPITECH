/*
** EPITECH PROJECT, 2024
** Duo Stumper 4
** File description:
** addtofridge.c
*/

#include "my.h"

/*static int is_an_ingredient(char *str)
{
    char ingredients = ["tomato", "dough", "onion",
    "pasta", "olive", "pepper", "ham", "cheese"];

    for (int i = 0; ingredients[i] != '\n'; i++)
        if (strcmp(ingredients[i], str) == 0)
            return 1;
    return 0;
}

void add_to_fridge(ingredients_t ingredients, char *input)
{
    char str[100];
    int nbr = 0;
    int count;

    for (int i = 12; input[i] != ' '; i++) {
        str[i] = input[i];
        if (str[i + 1] == ' ')
            count = i + 2;
    }
    for (int y = count; input[y] != '\0'; y++)
        nbr += atoi(intput[y]);
    if (is_an_ingredient(str)) {
        ingredients.str += nbr;
    }
    if (printf("'addToFridge %s %d': Invalid operation\n") < 0)
        return -1;
    return 0;
}
*/
